Todos os alunos devem criar um branch com o seu nome e mantar esse branch atualizado com os fontes de seus projetos.

############################################################
#             Comandos básicos GIT
############################################################

Um branch pode ser criado com o comando:

    git branch <nome do branch>

Uma vez criado o aluno pode entrar no seu branch utilizando o comando

    git checkout <nome do branch>

Após editar uma série de arquivos, para verificar quais estão
Para comitar algum arquivo no seu repositório local o aluno deve primeiro adicionar esse arquivo na lista de índice por meio do comando

    git add <nome do arquivo>

Para efetuar o comit o camando a ser utilizado será

    git commit

Após o commit ser efetuado será necessário enviar os arquivos par o respositório principal no GitHub via o comando:

    git push

############################################################
#             Configurações do ambiente Python
############################################################
Adicione no arquivo /etc/profile o endereço do diretório onde o Python esta instalado e o diretório de Scripts

export PATH=$PATH:/c/Python34
export PATH=$PATH:/c/Python34/Scripts

Pelo terminal do windows ou pelo git bash instale os seguintes pacotes pelo comando pip

pip install jsonrpclib-pelix
pip install rpyc
pip install python3-memcached
